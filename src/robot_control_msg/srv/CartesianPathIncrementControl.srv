# 笛卡尔空间多路点增量路径控制服务

# 左臂增量 waypoint（逐点累加到当前/上一点）
geometry_msgs/Pose[] left_waypoints
float64[] left_blend_radii

# 右臂增量 waypoint（逐点累加到当前/上一点）
geometry_msgs/Pose[] right_waypoints
float64[] right_blend_radii

# 速度和加速度参数
float64 vel
float64 acc

---
# 响应：操作结果
bool success
string message
